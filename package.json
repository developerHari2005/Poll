{
  "name": "fullstack-poll-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:frontend": "cd frontend && npm install && npm start",
    "start:backend": "cd backend && npm install && node server.js",
    "install-all": "npm install &&  npm install --prefix frontend  && npm install --prefix backend && npm run build --prefix frontend",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
